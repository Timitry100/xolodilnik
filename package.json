{
  "name": "xolodilnik",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram Mini App «Холодильник» — учёт продуктов и сроков годности",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix app install",
    "build": "npm --prefix app run build",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:app": "npm --prefix app run dev"
  }
}
